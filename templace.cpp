#include <bits/stdc++.h>

/*
vector<int> arr(N);
for (int i = 0; i < N; i++) cin >> arr[i];
sort(arr.begin(), arr.end());

set<int> distinctNumbers;

*/


int main() {
	int N;
	cin >> N;
	
	
	cout << ans << endl;
	return 0;
}